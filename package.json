{
  "name": "jamming",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "git-scripts": "^0.2.1",
    "surge": "^0.23.1"
  },
  "git": {
    "scripts": {
      "pre-push": "surge --project ./ --domain edrosillo-jamming.surge.sh"
    }
  }
}
